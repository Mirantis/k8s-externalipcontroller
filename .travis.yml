sudo: required
language: go
services:
    - docker
before_install:
  - echo "$TRAVIS_BRANCH"
  - git rev-parse --abbrev-ref HEAD
  - exit 1
  - mkdir -p _output/
  - sudo apt-get install -y git
install:
  - make get-deps
script: make test
