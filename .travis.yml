sudo: required
language: go
services:
    - docker
before_install:
  - sudo apt-get install -y git
install:
  - go get github.com/Masterminds/glide
  - glide install --strip-vendor
before_script:
  - ./prepare.sh
  - ./run_tests.sh -bi
script: ./run_tests.sh -r
