sudo: required
services:
  - docker
language: go
before_install:
  - mkdir -p _output/
  - sudo apt-get install -y git
  - sudo apt-get install libpcap-dev
install:
  - make get-deps
script:
  - make test
  - make docker-publish
